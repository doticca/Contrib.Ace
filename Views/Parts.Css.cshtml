@using Contrib.Ace.Models;
@{
    CssPart css = null;
    try{
        css = Model.Css;
    }
    catch {}
}
@if (css != null && !string.IsNullOrWhiteSpace(css.Css))
{
    <style>
@Html.Raw(css.Css)
    </style>
}